---
import { Astro } from 'astro/runtime/server/index.js';

// Redirige a la primera página de la paginación del blog
export const prerender = true;

export function getStaticPaths() {
  return [
    {
      params: {},
      props: {},
    },
  ];
}

export const GET = () => {
  return Astro.redirect('/index/');
};
---